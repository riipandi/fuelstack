{
  "name": "admin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "deploy": "vercel deploy dist --team=turborepo --confirm",
    "dev": "vite --host 0.0.0.0 --port 4100 --clearScreen false",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts*\""
  },
  "dependencies": {
    "@acme/ui": "workspace:*",
    "react-dom": "^18.2.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@acme/eslint-config-custom": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@types/react-dom": "^18.2.6",
    "@types/react": "^18.2.14",
    "@vitejs/plugin-react": "^4.0.1",
    "eslint": "^8.44.0",
    "typescript": "^5.0.4",
    "vite": "^4.3.9"
  }
}
